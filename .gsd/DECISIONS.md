# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 planning | architecture | Where iOS Explore tag and age filtering executes | Client-side in ExploreViewModel.applyClientFilters, post-fetch | events_enriched_v2 has no p_tag_slugs or p_age_min/p_age_max params. Web does the same — fetches a city/date window and filters client-side. Adding server-side params requires a DB migration with no user-visible benefit at current event volumes. | Yes — revisit if event volumes grow large enough that fetching 100+ events per page causes perceptible latency on cellular. | collaborative |
| D002 | M001 planning | architecture | iOS RPC version for event fetching | Migrate iOS SupabaseEventRepository from events_enriched (v1) to events_enriched_v2 | Web already uses v2. v2 adds cursor pagination, parent_tips, and is_outdoor. Staying on v1 means iOS silently misses enrichment data and falls further behind each v2 addition. The v2 param set is a superset of v1 — migration is mechanical. | No — v1 should be retired once all clients are on v2. | agent |
| D003 | M001 planning | architecture | Geocoding improvement strategy for centroid-stuck events | Widen _has_geocodable_address predicate in list_events_needing_enrichment — no city-centroid fallback write-back | Writing the centroid back re-flags the row as needs_coords and causes claim-queue starvation (documented in backfill-event-enrichment/index.ts comments). The better fix is making the predicate more precise so more rows become eligible for real geocoding. The last two migrations followed this strategy; this milestone continues it. | Yes — if a new geocoder provider (e.g. Google Maps) is adopted, the strategy may change. | agent |
| D004 | M001/S03 planning | architecture | Fate of the search_events RPC in the database | Deleted via DROP migration — not wired into any new TypeScript caller | Zero callers in apps/web/src, zero callers in edge functions or cron scripts. search_events_v2 covers the cursor-based path and is the canonical RPC. The v1 function carries dead schema surface with no upgrade path worth implementing at current event volumes and caller state. | No — search_events_v2 is the migration path; v1 has no unique functionality worth restoring. | agent |
| D005 |  | architecture | Fate of the search_events RPC in the database | Deleted via DROP migration — not wired into any new TypeScript caller | Zero callers in apps/web/src, zero callers in edge functions or cron scripts. search_events_v2 covers the cursor-based path and is the canonical RPC. The v1 function carries dead schema surface with no upgrade path worth implementing at current state. | No — search_events_v2 is the migration path; v1 has no unique functionality worth restoring. | agent |

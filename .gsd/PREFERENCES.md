---
version: 1
mode: solo
custom_instructions:
  - Terse execution mode. Do the work; avoid narration unless blocked or asked to explain.
  - This is a pnpm TypeScript monorepo. Use repo-local scripts and existing patterns. Prefer small, reviewable changes.
  - Before repo-scoped work, check the current branch and dirty files. Do not revert unrelated user changes.
  - For Node commands, prefer PATH=/Users/lecoqjacob/.nvm/versions/node/v24.11.1/bin:$PATH.
  - For frontend changes, keep UI consistent with existing components and verify with build/tests when practical.
  - For Supabase/database work, use migrations for durable schema changes.
  - Never guess Supabase CLI commands; check help when needed.
  - Prefer existing cached/public views and RPCs over direct system-table/function scans.
  - For timezone names, use public.timezone_names or public.pg_timezone_names, never pg_catalog.pg_timezone_names directly.
  - Verify remote/local behavior when changing deployed database paths.
  - For Railway/deploy work, use existing scripts such as scripts/deploy.sh.
  - Check deploy status/logs before calling work complete.
  - For narrow changes, run the smallest relevant check.
  - For shared/schema/API changes, run typecheck/tests for affected package plus DB verification.
  - For full gate requests, run pnpm run verify:full.
  - Final responses should be brief - changed files, verification, blockers only.
git:
  isolation: worktree
  main_branch: main
  auto_push: true
verification_commands:
  - pnpm test
  - pnpm run build
---

# GSD Project Preferences

Generated by `/gsd init`. Edit directly or use `/gsd prefs project` to modify.

See `~/.gsd/agent/extensions/gsd/docs/preferences-reference.md` for full field documentation.

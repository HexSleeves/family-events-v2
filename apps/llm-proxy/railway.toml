[build]
builder = "RAILPACK"
buildCommand = "node --check src/server.js"
watchPatterns = [
  "apps/llm-proxy/**",
]

[deploy]
startCommand = "node src/server.js"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
